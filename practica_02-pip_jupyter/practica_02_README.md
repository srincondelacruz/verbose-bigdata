# Práctica 02: Jupyter y Comandos PIP

Práctica para familiarizarse con los comandos básicos de `pip` y el uso de `pandas` dentro de un entorno Jupyter.

## 🚀 Cómo ejecutar esta práctica

Desde este directorio (`practica_02-pip_jupyter/`):

1.  **Crear el entorno** (si no existe):
    `python -m venv .venv`

2.  **Activar el entorno**:
    `.\.venv\Scripts\Activate.ps1`

3.  **Instalar dependencias** (Jupyter, Pandas, etc.):
    `pip install -r requirements.txt`

4.  **Lanzar Jupyter**:
    `jupyter notebook`

## 📒 Contenido

El notebook `practica_02_pip.ipynb` contiene todos los experimentos con los comandos `pip list`, `pip show`, `pip freeze`, etc.