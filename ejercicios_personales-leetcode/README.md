# Ejercicios Personales: LeetCode y Algoritmos (DS&A)

Este directorio es un espacio de trabajo personal para la práctica de más de 100 desafíos de programación en Python.

---

## 🎯 Objetivo

El objetivo de este proyecto es doble: **mejorar la capacidad de escribir código eficiente** (eficiencia O(n)) y dominar las estructuras de datos y algoritmos fundamentales de Python (DS&A).

## 🗂️ Contenido y Clasificación de Problemas

Esta colección de ejercicios ha sido clasificada por tema para facilitar un estudio estructurado:

### I. Fundamentos de Python (Nivel 1)
*(Preguntas 1-5, 23-32)*

**Enfoque:** Sintaxis básica, uso de `range()`, `input()`, `print()`, operadores (`%`, `**`), y definición de funciones simples y clases.
* **Ejemplos clave:** Factorial (Q2), Crear diccionario `(i, i*i)` (Q3), Clases con métodos `getString`/`printString` (Q5).

### II. Manipulación de Contenedores y Estructuras (Nivel 1-2)
*(Preguntas 4, 33-43, 86, 93-94)*

**Enfoque:** Creación, modificación y slicing de `listas`, `tuplas` y `diccionarios`. Uso de `list.append()` y `dict.items()`.
* **Ejemplos clave:** Convertir entre lista y tupla (Q4), Generar diccionarios con bucles (Q34), Obtener slices de listas/tuplas (Q38, Q42).

### III. List Comprehension y Funcional (Nivel 2)
*(Preguntas 16, 45-49, 76, 79, 87-92)*

**Enfoque:** Dominio de las funciones de orden superior (`map`, `filter`) y la sintaxis eficiente de *comprehensions*.
* **Ejemplos clave:** Cuadrado de números pares con `map()` y `filter()` (Q47), Filtrado y remoción de elementos por índice o valor (Q87, Q89, Q92), Generación de listas aleatorias con *comprehension* (Q79).

### IV. Cadenas de Texto y Expresiones Regulares (Nivel 2-3)
*(Preguntas 8, 9, 10, 13, 14, 58-60, 97-98)*

**Enfoque:** Manipulación de *strings* (`upper()`, `split()`, `join()`), ordenamiento alfanumérico, y uso de la librería `re` (Regex) para validación de emails y extracción de datos.
* **Ejemplos clave:** Ordenar palabras (Q8), Remover duplicados y ordenar (Q10), Extraer nombre/compañía con Regex (Q58, Q59).

### V. Algoritmos y Problemas Matemáticos (Nivel 2-3)
*(Preguntas 6, 7, 12, 15, 21, 64-67, 72, 100)*

**Enfoque:** Algoritmos clásicos y lógica matemática.
* **Ejemplos clave:** Fórmula de la raíz cuadrada (Q6), Generación de matrices 2D (`i*j`) (Q7), Secuencia de Fibonacci (Q66), Búsqueda binaria (Q72), Problema de las cabezas y patas (Q100).

### VI. POO, Excepciones y Generadores (Nivel 1-3)
*(Preguntas 5, 20, 24, 25, 50-57, 68, 69)*

**Enfoque:** Conceptos avanzados como Programación Orientada a Objetos (POO), Herencia, Métodos Estáticos, manejo de `try/except` y uso de `yield` para generadores.
* **Ejemplos clave:** Clases con herencia (Q51, Q54), Clases con método `area()` (Q52, Q53), Uso de generadores (Q20, Q68).

---

## 🚀 Siguiente Paso

El archivo **`EJERCICIOS_LEETCODE.md`** contiene todos los enunciados. Las soluciones se implementarán en scripts `.py` separados dentro de esta carpeta.